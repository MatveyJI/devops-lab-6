apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: work-app-sm
  labels:
    release: monitoring-stack # Это связывает его с установленным Prometheus
spec:
  selector:
    matchLabels:
      app: work-app
  endpoints:
  - port: http
    path: /q/metrics
    interval: 1s